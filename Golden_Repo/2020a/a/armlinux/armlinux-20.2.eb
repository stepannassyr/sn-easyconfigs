# This is a thin wrapper that depends on Modules already being installed and in the MODULEPATH
import os
easyblock = 'Bundle'
name = 'armlinux'
version = '20.2'

homepage = 'http://www.arm.com'
description = """
ARM Compiler for Linux (armclang, armclang++, armflang)
"""


toolchain = SYSTEM
osprefix = os.getenv("ARMOSPREFIX")
dependencies = [('Generic-AArch64/%s/arm-linux-compiler/%s' % (osprefix,version),     EXTERNAL_MODULE)]


moduleclass = 'compiler'
